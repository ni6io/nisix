# BOOTSTRAP

1. Confirm `IDENTITY.md` and `SOUL.md` are present and valid.
2. Confirm tool policy defaults are safe before external calls.
3. Confirm routing bindings for active channels.
4. Confirm model provider and credentials are configured.
5. Confirm telemetry and session persistence are writable.
